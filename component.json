{
  "name": "json-fallback",
  "repo": "component/json",
  "description": "JSON parser / stringifier fallback",
  "version": "0.0.1",
  "keywords": ["json", "fallback"],
  "dependencies": {},
  "scripts": ["index.js"],
  "development": {
    "component/assert": "*"
  }
}
